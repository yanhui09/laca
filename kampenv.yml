channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python
  - mamba
  - snakemake-minimal=7.7.0 # can't solve strict chanel priority check; some envs could only be resolved in flexible priority
  - pip
  - click
  - versioneer
  - pandas
